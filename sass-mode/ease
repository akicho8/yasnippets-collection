# --
`(yas-choose-value '(
"ease              // 初期値です。開始時と終了時は緩やか"
"ease-in           // 開始時は緩やかに変化、終了に近づくと早く"
"ease-out          // 開始時は早く変化し、終了時は緩やか"
"ease-in-out       // 開始時と終了時は、かなり緩やか"
"linear            // 開始から終了まで一定に変化"
"step-start        // 開始時に最終の状態"
"step-end          // 終了時に最終の状態"
"steps(...)        // 指定した正数の段階で変化"
"cubic-bezier(...) // 変化の度合いを任意で指定"
))`
