# --
`(yas-choose-value '(
order(:position)
order(position: :asc)
order(arel_table[:created_at].desc])
order(arel_table[:updated_at].desc])
order([arel_table[:updated_at].desc, arel_table[:id].desc])
order([\"FIELD(name, ?)\", list])
))`

